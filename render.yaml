services:
  - type: web
    name: ai-agent-for-automated-website-testing
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python -m playwright install chromium
    startCommand: gunicorn app:app